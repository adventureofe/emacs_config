# -*- mode: snippet -*-
# name: factors
# key: factors
# --
void find_factors(int n)
{
    for(int i = 1; i <= n/2; i++)
    {
        if(n % i == 0)
        {
            printf("%d,", i);
        }
    }
    printf("%d \n", n);
}
