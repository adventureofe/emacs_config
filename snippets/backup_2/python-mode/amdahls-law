# -*- mode: snippet -*-
# name: amdahls-law 
# key: amdahls-law 
# --

# (FORMULA) S_latency(speedup) = 1 / ( (1 - proportion) + ( proportion / speedup ) )
amdahls_law_ratio = lambda ratio, speedup : 1 / ( (1 - ratio) + (ratio / speedup))
amdahls_law_percentage = lambda percentage, speedup : 1 / ( (1 - (percentage / 100) ) + ( (percentage / 100) / speedup) )

