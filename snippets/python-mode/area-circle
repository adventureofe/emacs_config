# -*- mode: snippet -*-
# name: area-circle
# key: area-circle
# --
import math

area_circle_radius = lambda radius : math.pi * pow(radius, 2)

area_circle_circumference = lambda circumference : pow(circumference, 2) / (4 * math.pi)