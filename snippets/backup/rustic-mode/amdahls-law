# -*- mode: snippet -*-
# name:amdahls-law
# key:amdahls-law
# --
fn amdahls_law_ratio(p:f64, s:f64) ->f64
{
    return 1.0 / (1.0 - p + p/s);
}

fn amdahls_law_percentage(p:f64, s:f64) ->f64
{
    return 1.0 / (1.0 - (p/100.0) + (p/100.0)/s);
}
